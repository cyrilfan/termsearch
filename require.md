# 术语速查工具（TermSearch）需求文档

## 1. 项目概述

一个 Windows 桌面小工具，用于快速查询缩写术语的全称和解释。通过全局热键随时唤起一个输入框，输入缩写后立即在本地术语表中查找匹配项并展示结果。

- **使用者**：当前仅自用；后续会考虑对外发布（如开源/分享给同事），但架构不需要为此提前做过度设计（不需要国际化框架、不需要安装包等）。
- **核心场景**：用户在任意应用中工作时，按下热键 → 弹出输入框 → 输入缩写 → 立即看到全称与解释 → 按 Enter 复制全称并关闭，或按 Esc 直接关闭。

## 2. 目标平台

- 仅支持 **Windows 10 (1809+) / Windows 11**，不需要兼容 Win7/Win8.1。
- 可以使用较新的 Windows API 与运行时。

## 3. 分发形态

- **单文件绿色版 exe**：双击即用，不需要安装程序，不需要管理员权限。
- 不做安装包，这个留待未来真正对外发布时再评估；软件更新见第 15 节（这项已经做了，不算"留待评估"）。
- 程序应能自包含运行（避免要求用户额外安装运行时/框架；若技术栈本身需要运行时，需在文档中说明并尽量降低门槛）。

## 4. 快捷键

### 4.1 全局热键

- 默认组合键：**`Alt+Space`**。
- 热键值必须可通过配置文件自定义（见第 8 节配置文件），避免与其他常驻软件冲突时无法修改。
- 若热键注册失败（如被其他程序占用），需要有明确的失败提示（例如托盘气泡通知或日志），不能静默失败。
- 这是系统级全局热键（在任意程序中都能唤起弹窗），需要向操作系统注册。

### 4.2 新增全称快捷键（弹窗内局部按键）

- 默认组合键：**`Shift+Enter`**。
- 用于第 7 节所述的"已有匹配结果，但都不是想要的，追加一条新全称"场景，只在弹窗拥有焦点时响应，**不需要**向操作系统注册全局热键。
- 同样可通过配置文件自定义（见第 8 节），不能和全局热键（4.1）设成同一个组合。

### 4.3 编辑词条快捷键（弹窗内局部按键）

- 默认组合键：**`Ctrl+Shift+Enter`**。
- 用于第 7 节所述的"当前有选中的匹配结果，想直接改它的全称/解释"场景，只在弹窗拥有焦点、且当前确实有匹配结果时才生效；完全没有匹配结果时按这个快捷键没有意义（应该走新增流程）。只在弹窗拥有焦点时响应，**不需要**向操作系统注册全局热键。
- 同样可通过配置文件自定义（见第 8 节），不能和全局热键（4.1）、新增全称快捷键（4.2）中的任何一个设成同一个组合。

### 4.4 应用内修改快捷键

- 全局热键、新增全称快捷键、编辑词条快捷键都从托盘菜单打开各自的小对话框（"修改全局热键..." / "修改新增全称快捷键..." / "修改编辑词条快捷键..."），交互方式一致：用户直接按下新的组合键（需包含至少一个 Ctrl/Alt/Shift/Win + 一个按键），对话框实时显示当前录制到的组合。
- 点击"保存"时校验并尝试应用新组合：
  - 全局热键：先尝试向系统注册，成功才写回 `config.json`；失败（多半被其他程序占用）则在对话框内显示错误提示，不关闭对话框，并确保原热键继续可用。
  - 新增全称快捷键、编辑词条快捷键：都不需要系统注册，但要校验不能和另外两个快捷键中的任何一个相同；校验通过即写回 `config.json`。
  - 三个方向两两互相校验：改任意一个，都不能和当前另外两个中的任意一个相同。
- Esc 或点击"取消"关闭对话框且不做任何修改。

## 5. 核心交互流程

1. 用户按下全局热键。
2. 屏幕**默认固定位置**（屏幕中央偏上）弹出一个小窗口，包含一个文本输入框，自动获得焦点。
3. 用户输入缩写文本（不区分大小写）。
4. 程序在术语表中做**三档匹配**（见第 6 节），实时（每次按键后）更新候选/结果显示区域，显示在输入框下方。
5. 结果展示后，**不自动复制**任何内容。
6. 用户按 **Enter**：
   - 若当前有匹配结果（存在唯一或已选中的候选项），将其“全称”复制到系统剪贴板，然后关闭窗口。
   - 若无匹配结果（提示“暂未添加，待补充”状态），Enter 触发第 7 节所述的快捷补充流程，而不是复制。
7. 用户按**新增全称快捷键**（默认 `Shift+Enter`，可在托盘菜单里配置）：无论当前有没有匹配结果，都以当前输入框文本作为缩写，触发第 7 节的快捷补充流程——用于"这个缩写已经有全称，但都不是我要的，想追加一条新的"的场景。
8. 用户按**编辑词条快捷键**（默认 `Ctrl+Shift+Enter`，可在托盘菜单里配置）：只有当前有匹配结果时才生效，以当前选中的候选项为目标，触发第 7 节的编辑流程——用于"这条全称/解释写错了/想改一下"的场景。
9. 用户按 **Esc** 或**点击窗口外部区域**：直接关闭窗口，不做任何复制操作。
10. 窗口没有系统标题栏，顶部有一条不可见的细拖拽条（约 10px 高，贯穿整个窗口宽度，悬停时鼠标变为移动光标），按住左键拖动可以把窗口移到任意位置。拖拽后的位置只在**当次程序运行期间**记住（同一次运行内多次弹出都保持在拖过去的位置），不写入 `config.json`；程序重启后回到默认固定位置。拖拽/弹出时都会把窗口坐标夹紧在当前所有显示器组成的可见范围内，避免拖出屏幕后再也找不到窗口。

## 6. 匹配规则

采用**三档匹配**，按优先级从高到低依次是：

1. **全部匹配（精确匹配）**：输入内容和缩写完全一致（大小写不敏感）。
2. **前缀匹配**：缩写以输入内容开头（大小写不敏感）。例如输入 `AP` 会匹配所有以 `AP` 开头的缩写（如 `API`、`APP`）。
3. **模糊匹配**：输入的每个字符按顺序（可跳过中间字符）出现在缩写里即算命中，类似 fzf/Sublime 命令面板的子序列匹配。命中越连续、起始位置越靠前，排序越靠前。

同一个缩写只会归入其中匹配优先级最高的一档，不会同时出现在多档里；三档内部各自排序（全部/前缀匹配按字母序，模糊匹配按匹配质量得分）后依次拼接展示。模糊匹配命中的字符在结果列表里会高亮显示。

- 匹配到多个候选时，以列表形式全部展示（见第 9 节数据结构中"一对多"的处理）；前缀/模糊两档各自最多展示前 30 条（按各自排序规则取最靠前的），超出部分直接丢弃不展示——结果区域本来就只能看到几条，短查询（比如单个字母）命中成百上千条时，多出来的既看不到也用不上，只会拖慢每次按键的响应（性能问题，2026-09 修复）。
- 若完全没有匹配到任何术语，显示提示文案：**"暂未添加，待补充"**，并进入第 7 节的快捷补充引导状态。
- 输入为空时不查询、不显示结果区域（或显示空态）。

## 7. 快捷补充与编辑流程

新增和编辑共用同一套输入界面（同一个窗口内完成，不弹新窗口），区别只在于"缩写"字段能不能改、以及提交后是新增一条还是原地替换。

### 7.1 新增

两个入口都会进入新增模式：

- **完全没有匹配结果**时，按 **Enter**；
- **已有匹配结果，但都不是想要的**时，按**新增全称快捷键**（默认 `Shift+Enter`，可配置，见第 4 节）。

流程：

1. 无匹配时界面上给出明确提示，例如："暂未添加，待补充（按 Enter 立即添加）"。
2. 触发后，窗口内追加/切换出"缩写"、"全称"和"解释"三个输入字段。"缩写"默认填入当前输入框的文本并整体选中，但**允许修改**——例如查询时输入的是小写 `abc`，想存成 `ABC`，可以直接改。
3. 用户填写缩写、全称与解释后，按 Enter（或提供的"保存"操作）提交：
   - 提交时以"缩写"输入框的最终内容为准（不是查询时输入的原文），若为空则不提交、光标留在该字段。
   - 将新词条写回本地 `terms.json` 文件（见第 8 节数据结构）；若该缩写已存在，追加为该缩写下的新一条全称，不覆盖已有条目。
   - 关闭窗口（或返回可继续查询的状态，具体交互可由实现者选择更顺手的一种，但写入行为必须发生在关闭前）。
4. 本流程不做重复校验——同一缩写下允许出现内容完全相同的多条全称，交由用户自行判断，维护成本换来实现简单。

### 7.2 编辑已有词条

- 入口：当前**有匹配结果**时，按**编辑词条快捷键**（默认 `Ctrl+Shift+Enter`，可配置，见第 4 节），以当前选中的候选项为编辑目标。
- 触发后同样切换出"缩写"、"全称"、"解释"三个字段，但：
  - "缩写"字段填入该词条所属的缩写，**只读、不可修改**（改缩写等于换 key，属于更复杂的操作，本版本不支持；真要改缩写，走直接编辑 `terms.json`）。
  - "全称"、"解释"字段填入该词条**当前的**内容（而不是空的），供用户直接修改。
- 用户修改后按 Enter（或"保存"）提交：**原地替换**该词条的全称/解释，不新增条目、不影响同一缩写下的其他释义。
- 若提交时发现原词条已经不在术语表里了（比如提交前术语表被外部编辑/替换过），不假装保存成功：停留在当前界面并给出提示，不写入、不关闭窗口，交由用户自行决定重新查询或放弃。

### 7.3 两种模式共通

- 若用户在补充/编辑状态下按 Esc，取消本次操作，直接关闭窗口，不写入任何数据。

## 8. 数据存储与配置

### 8.1 术语数据文件

- 文件名：`terms.json`
- 位置：**程序所在目录**下（绿色软件思路，整个文件夹可整体拷贝/备份/同步）。
- 程序需要**监听该文件的变化**（文件系统监视），一旦用户用外部编辑器手动修改并保存该文件，程序应自动重新加载数据，无需重启程序。
- 程序自身写入（快捷补充流程）也应触发同样的重新加载逻辑，保证内存中的数据与文件一致。

### 8.2 数据结构

需要支持**一个缩写对应多条释义**（因为同一缩写在不同语境下可能有不同全称）。建议结构如下（具体字段名由实现者决定，需保持语义等价）：

```json
{
  "API": [
    { "fullName": "Application Programming Interface", "description": "应用程序编程接口" }
  ],
  "APP": [
    { "fullName": "Application", "description": "应用程序" }
  ],
  "PM": [
    { "fullName": "Product Manager", "description": "产品经理" },
    { "fullName": "Project Manager", "description": "项目经理" }
  ]
}
```

- 查询时对 key 做大小写不敏感的三档匹配（见第 6 节）。
- 一个 key 对应的释义数组中如有多条，需要在结果区域全部列出（可编号/分行展示）；新增全称快捷键写入时同样追加到该数组，不覆盖已有条目。

### 8.3 配置文件

- 需要一个独立的配置文件（例如 `config.json`，位置与 `terms.json` 同目录）存放可调整项，至少包含：
  - 全局热键组合（默认 `Alt+Space`）
  - 新增全称快捷键组合（默认 `Shift+Enter`，见 4.2 节）
  - 编辑词条快捷键组合（默认 `Ctrl+Shift+Enter`，见 4.3 节）
  - 开机自启动开关状态（默认关闭，见第 10 节）
- 配置文件同样应支持外部编辑后生效（重启生效即可，不强制要求配置项热更新）。

### 8.4 数据安全

- **保存时原子写入**：`terms.json`/`config.json` 保存时先写到同目录下的临时文件，再整体替换正式文件，不直接覆盖写。这样保存过程中如果程序崩溃/断电/被杀，正式文件要么完全没被动过，要么已经是替换后的新内容，不会留下写到一半的半截 JSON。
- **加载失败不清空旧数据**：`Load()` 解析失败（文件损坏、外部编辑写坏等）时，保留调用前那份还能用的旧数据继续用，不会直接换成空表/默认配置——除非这是程序生命周期里第一次加载（这时本来就没有"旧数据"可保留，自然还是空表/默认配置，这是唯一的例外情况）。
- **留档**：加载失败时会把这份读不出来的文件原样复制一份留档（文件名加 `.corrupted-<时间戳>` 后缀，不删除/移动原文件），方便事后手动抢救内容；下一次正常保存会自然把原文件覆盖成好数据。
- **提醒**：`terms.json` 加载失败（外部编辑触发的热重载失败）会用托盘气泡提醒一次，避免"其实数据早就没在更新，只是没人告诉你"这种沉默失败长期不被发现。`config.json` 目前只在程序启动时加载一次、没有热重载入口，所以这个提醒实际上只有在未来给 `config.json` 也加上热重载触发点之后才会真正用上。

## 9. 系统托盘与后台驻留

- 程序常驻后台运行，提供**系统托盘图标**。
- 托盘右键菜单至少包含：
  - 打开 `terms.json`（用默认程序打开，方便手动编辑）
  - 重新加载术语表
  - 修改全局热键...（打开一个小对话框，见 4.1 节）
  - 修改新增全称快捷键...（打开一个小对话框，见 4.2 节）
  - 修改编辑词条快捷键...（打开一个小对话框，见 4.3 节）
  - 开机自启动（可勾选开关，默认关闭，勾选后写入配置文件并注册/取消 Windows 启动项）
  - 退出程序
- 关闭主输入窗口（Esc/点外部/Enter 后）不应退出程序，只是隐藏窗口，程序仍驻留在托盘。

## 10. 单实例限制

- 程序必须保证**单实例运行**。
- 若程序已在运行时，用户再次双击 exe 或再次触发热键：
  - 双击 exe：应激活/聚焦已运行实例（而不是报错或静默退出），或至少不产生第二个进程。
  - 再次触发热键：正常触发弹出输入框逻辑（这本来就是同一个运行中实例响应热键，天然满足单实例要求）。

## 11. 界面语言

- 当前版本界面文案（提示语、按钮、菜单等）全部使用**中文**。
- 不需要设计国际化/多语言架构，未来若真正对外发布且有明确的英文用户需求，再另行评估。

## 12. 非功能性要求

- **启动/响应速度**：作为热键触发的常驻工具，弹窗必须做到接近瞬时响应（几十到一两百毫秒级别），不能有明显的加载延迟或白屏；打字查询时同理，每敲一个字到结果刷新之间不应有能感知到的卡顿（配置较弱的设备上更容易暴露，2026-09 修复过一次：窗口的 `AllowsTransparency`（换圆角外观的必要代价）叠加 `DropShadowEffect` 会强制整个视觉树走软件渲染，且弹窗高度随结果数量变化，等于每敲一个字都要重新走一遍软件合成——已去掉阴影效果；同时短查询命中的候选量也已封顶，避免无谓构建/渲染看不到的结果）。
- **资源占用**：后台常驻时应尽量轻量（低内存、无持续 CPU 占用），避免影响日常使用体验。
- **健壮性**：`terms.json` 或 `config.json` 缺失、为空、格式错误时，程序不应崩溃，应使用合理的默认值/空数据启动，并给出可感知的提示（如托盘气泡或日志文件）。

## 13. 技术栈

由实现者（agent/开发者）自行决定，但需满足以下约束：

- 能实现 Windows 全局热键注册。
- 能生成单文件、无需安装、启动迅速的 Windows 桌面程序。
- 能实现自定义无边框弹窗、系统托盘图标、文件系统监视（watch `terms.json`）。
- 优先考虑对第 12 节非功能性要求（尤其是启动速度和资源占用）友好的方案。

## 14. 明确排除在本次范围之外（Out of Scope）

- 安装包、代码签名。
- 多语言/国际化界面。
- 拼音匹配、编辑距离（打错字容错）等超出"子序列模糊匹配"范围的算法；全文搜索 FullName/Description 正文（当前匹配只针对缩写 key 本身，见第 6 节）。
- 云同步、多设备共享术语表。
- 术语表的可视化管理界面（列出全部词条、批量增删改的列表页）——本版本维护数据的方式仅限于：查询时快捷补充新词条（7.1 节）、编辑当前查到的某一条全称/解释（7.2 节，仅限单条、不支持改缩写/删除）、直接编辑 `terms.json`。

## 15. 软件更新

- **自动化程度**：半自动——程序检测到新版本后提示，用户确认后才下载、替换、重启，不做全自动静默更新（万一新版本有问题，静默换掉能用的旧版本风险更大）。
- **检查时机**：程序启动时异步检查一次，不阻塞热键注册/弹窗响应速度；检查失败（无网络、GitHub 不可达等）静默处理，不打断正常使用。托盘菜单另外提供"检查更新..."手动触发一次，无论有没有更新都要给出反馈（有更新弹提示；没有更新要提示"已是最新版本"，不能像没反应一样）。
- **更新源**：GitHub Release（当前仓库 `cyrilfan/termsearch`，必须是 Public——GitHub API 不允许匿名访问私有仓库的 Release 信息，包括下载链接）。查询 `releases/latest`，用其 `tag_name`（`v0.1`、`v0.2` 这种两段式）跟当前程序内置的版本号比较，只比主版本号和次版本号。
- **发布流程**：这个功能只负责"检测 + 下载已发布的版本"，不负责"发布"本身——发布新版本仍然是手动流程（改代码 → `dotnet publish` → 打 zip → `gh release create`），跟这个功能是两件事。每次发布前需要把程序内置的版本号改成即将打的那个 tag 对应的号。
- **确认与下载**：检测到新版本后弹出一个非模态对话框（不阻塞正常查词），展示版本号和 Release 说明文字；用户点"立即更新"才会真正下载 Release 里的 zip 附件、显示下载进度；点"以后再说"直接关闭，不做任何改动，下次启动会重新检查。
- **应用更新（替换正在运行的 exe）**：Windows 下运行中的 exe 不能覆盖自己。下载解压出新 exe 后，生成一个独立的脚本，等本进程退出后再把新 exe 覆盖到当前安装位置、重新启动、然后自我删除；主程序自己只负责启动这个脚本，然后正常退出（走已有的退出流程，释放热键/互斥体等）。
- **数据安全**：整个更新过程只碰 exe 本身，绝不动 `terms.json` / `config.json`（用户数据和配置），不会因为更新丢失本地术语表或配置。
- **清理与失败提醒**：下载解压产生的临时文件（zip + 解压目录）在替换脚本成功覆盖 exe 后会自动删除，不会在系统临时目录里越攒越多；如果下载/解压阶段就失败（没网络、zip 里没有 exe 等），当场在更新对话框里提示，同时清理已下载的部分。如果失败发生在替换脚本里（比如新 exe 被占用、权限不足导致覆盖失败），脚本会保留这份安装包不删（方便排查），并把原来的旧版本重新拉起来，下次启动时用托盘气泡提醒一次"上次自动更新失败"，提醒后不再重复弹。
